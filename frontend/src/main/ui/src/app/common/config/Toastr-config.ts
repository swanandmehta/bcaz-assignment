import { ComponentType, GlobalConfig, ProgressAnimationType, ToastrIconClasses } from 'ngx-toastr';

export class ToastrConfig {
    maxOpened = 10;
    autoDismiss = true;
    closeButton = true;
    positionClass = "toast-bottom-right";
}