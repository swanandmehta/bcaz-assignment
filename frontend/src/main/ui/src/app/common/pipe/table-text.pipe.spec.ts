import { TableTextPipe } from './table-text.pipe';

describe('TableTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TableTextPipe();
    expect(pipe).toBeTruthy();
  });
});
